<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/tabsScrollColor</item>
    </style>
    <style name="NoTitleTheme" parent="AppTheme">
        <item name="android:windowNoTitle">true</item> <!-- Hides the Action Bar -->
        <item name="android:windowFullscreen">false</item> <!-- Hides the status bar -->

    </style>
    <style name="AppTheme.TransparentActivity">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="TextAppearance.Sudo.FloatLabel" parent="android:TextAppearance.Small">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="myToolbarNavigationButtonStyle" parent="@style/Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:minWidth">0dp</item>
        <!--<item name="android:paddingTop">7dp</item>-->
        <!--<item name="android:paddingRight">7dp</item>-->

        <item name="android:scaleType">centerInside</item>
    </style>



    <!-- Animation for dialog. Slide in left out right-->
    <style name="DialogAnimationSlideLeftRight">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="FullScreen" parent="AppTheme">
        <item name="android:windowNoTitle">true</item> <!-- Hides the Action Bar -->
        <item name="android:windowFullscreen">true</item> <!-- Hides the status bar -->

    </style>


    <style name="DialogAnimationTest">
        <item name="android:windowEnterAnimation">@anim/anim_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/anim_dialog_out</item>
    </style>

    <style name="SpinnerButtonStyle" parent="android:Widget.Holo.Spinner">

        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
    </style>

    <style name="CheckBox" parent="Widget.AppCompat.CompoundButton.Switch">

         <item name="android:color">@drawable/bg_checkbox</item>
     </style>





</resources>
